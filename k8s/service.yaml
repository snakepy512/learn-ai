apiVersion: v1
kind: Service
metadata:
  name: cicd-demo
  namespace: cicd-demo
spec:
  selector:
    app.kubernetes.io/name: cicd-demo
  ports:
    - name: http
      port: 80
      targetPort: http
  type: ClusterIP